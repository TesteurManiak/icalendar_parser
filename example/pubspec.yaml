name: example
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  args: ^2.4.1
  icalendar_parser:
    path: ..

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
